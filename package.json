{
  "name": "hackmanila-gatekeeper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.9"
  },
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "node --test tests/*.test.mjs",
    "next:install": "npm install --save-exact next@latest react@latest react-dom@latest",
    "next:dev": "next dev",
    "next:build": "next build",
    "next:start": "next start",
    "smoke": "node scripts/smoke.mjs"
  }
}
